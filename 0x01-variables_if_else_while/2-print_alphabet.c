#include <stdio.h>
/**
 * main - print if the number is postive, zero, or negative
 *
 * Return: Always (Success)
 */
int main(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
